\chapter{Evaluation and Discussion of Results}
\label{cha:results}

\section{Overview}
% Brief orientation: how to read the plots, what each output type reveals,
% and cross-references to the experiment structure
% (Sec.~\ref{sec:exper:structure}).

\section{Baseline Suites}
\label{sec:results:baseline}
A central diagnostic in the baseline suites is the slope of the cumulative
regret curve. Let $r_t = f^{*}-f(x_t^{\mathrm{best}})$ denote the incumbent
instantaneous regret after evaluation $t$, and let
$\mathrm{\acrshort{cr}}(T)=\sum_{t=1}^{T} r_t$. Then
$\mathrm{\acrshort{cr}}(T)-\mathrm{CR}(T-1)=r_T$. Thus, a persistent
positive slope in the
linear cumulative-regret plot is direct evidence that the incumbent remains
suboptimal. When the best-so-far solution has stagnated at a local optimum, on a
plateau, or in a deceptive basin, the slope estimates the residual fitness gap
to the true optimum.

Conversely, if $\mathrm{\acrshort{cr}}(T)$ approaches a horizontal
asymptote $y=k$ as
$T\to\infty$, then $r_T\to 0$; on the finite benchmark landscapes used here,
this corresponds to eventual discovery of a global optimum. In finite-budget
experiments the asymptote can only be approximated, but the approximation
improves as the budget extends beyond the typical hitting time. The intercept
$y_0$ should therefore be read as the cumulative cost paid before convergence:
it is the area under the incumbent-regret trajectory, not merely the time of the
first optimum. Hence, two algorithms can have different interpretations under
\acrshort{ttfo} and cumulative regret. An algorithm may reach the optimum
sooner on average but accumulate more regret early in the run, whereas another
may converge more cautiously through better intermediate incumbents and finish
with a smaller $y_0$.
A central diagnostic in the baseline suites is the slope of the
cumulative regret curve. Let $\rho_t^{\mathrm{inc}} = f^{*} -
f(\hat{x}_t)$ denote the incumbent instantaneous regret after
evaluation $t$, where $\hat{x}_t = \operatorname{arg\,max}_{s \leq t}
f(x_s)$ is the best solution found so far, and let
$\mathrm{CR}(T) = \sum_{t=1}^{T} \rho_t^{\mathrm{inc}}$. Since
$\mathrm{CR}(T) - \mathrm{CR}(T-1) = \rho_T^{\mathrm{inc}}$, a
persistent positive slope in the linear cumulative-regret plot is
direct evidence that the incumbent remains suboptimal !TODO: ref trap, jump CR plots!. When the
best-so-far solution has stagnated at a local optimum, on a plateau,
or in a deceptive basin, the slope at time $T$ estimates the residual
fitness gap $f^{*} - f(\hat{x}_T)$ to the true optimum.

Conversely, if $\mathrm{CR}(T)$ approaches a horizontal asymptote
$C = \lim_{T \to \infty} \mathrm{CR}(T)$ as $T \to \infty$, then
$\rho_T^{\mathrm{inc}} \to 0$, which on the finite benchmark
landscapes studied here corresponds to eventual discovery of the
global optimum !TODO: ref
OneMax, LeadingOnes, TwoMax!. In finite-budget experiments the asymptote can only be
approximated, but the approximation improves as the budget extends
beyond the typical hitting time. By Corollary~\ref{cor:asymptotic-regret},
$C = \sum_{v=1}^{f^{*}} \mathbb{E}[\tau_v] - f^{*}$, so the
asymptote is determined by the sum of expected first-hitting times
across \emph{all} intermediate fitness levels, not only the terminal
one. It should therefore be read as the total cumulative cost paid
before convergence: the area under the entire incumbent-regret
trajectory!TODO: ref BinVal!. This distinguishes it from time-to-first-optimum
(\acrshort{ttfo}), which captures only the moment of final arrival.

All experiments in this section use $n = 100$, except for
\textsc{BinVal} at $n = 50$\footnote{The restriction arises because
\textsc{BinVal}'s optimum is $2^n - 1$: at $n = 100$ this value
exceeds the precision range of IEEE~754 double-precision
floating-point arithmetic, introducing rounding error into the regret
computation. At $n = 50$ the optimum $2^{50} - 1 \approx 1.13 \times
10^{15}$ is representable exactly.}.

\subsection{Suite 01: Algorithm-Family Comparison}
\label{sec:results:suite01}

\subsubsection{Aggregate Regret and Convergence}
% regret_curves (log-log), convergence_probability, comparison_heatmap
% Key question: at which budget does each family overtake the others?

\subsection{Suite 01: Algorithm-Family Comparison}
\label{sec:results:suite01}
\subsubsection{Regret Distributions and TTFO}
% regret_boxplots, performance_profile, ttfo_distribution at budget_for_plots
% Pairwise annotations (vs. RLS)
For the simpler \textsc{OneMax} and \textsc{TwoMax} instances, the relative ordering of mean algorithm performance aligns with the horizontal convergence points of the median incumbent \acrshort{cr} curves !TODO: ref onemax, twomax cr inc plots!. However, on \textsc{LeadingOnes} and \textsc{BinVal}, a structural inversion emerges !TODO: ref leadingones, binval!: the algorithms reach the global optimum in the order \acrshort{rls}, \acrshort{sa}-Log (d=2.0d=2.0
d=2.0), \acrshort{rlse}, then (1+1)(1+1)
(1+1)-\acrshort{ea}, yet their \acrshort{cr} asymptotes are ordered in reverse

This inversion follows directly from Corollary~\ref{cor:asymptotic-regret}:
the asymptote $C = \lim_{T \to \infty} \mathbb{E}[\mathrm{CR}(T)] = \sum_{v=1}^{f^{*}} \mathbb{E}[\tau_v] - f^{*}$ is governed by first-hitting times across \emph{all} intermediate fitness
levels. \acrshort{rls}'s greedy trajectory
reaches $f^{*}$ efficiently but lingers at lower fitness levels during
its ascent, inflating the per-level $\mathbb{E}[\tau_v]$ terms and
therefore $C$. The $(1+1)$-\acrshort{ea}'s multi-bit mutation
occasionally promotes the incumbent earlier in the run, reducing those
intermediate contributions and shrinking $C$, despite the later arrival
at $f^{*}$.

For \textsc{Jump} ($k=3$) and \textsc{Trap} ($k=10$), all algorithms
remain trapped in suboptimal basins within the given budget, so
$P(\mathrm{opt}) \approx 0$ and \acrshort{ttfo} is undefined for the
vast majority of runs. The incumbent \acrshort{cr} curves nevertheless
retain diagnostic structure. Each curve grows at an approximately
constant rate, confirming stagnation of the incumbent; the slope at
time $T$ estimates the residual fitness gap $\rho_T^{\mathrm{inc}} =
f^{*} - f(\hat{x}_T)$, so a steeper slope indicates that the
algorithm is trapped further from the optimum and is therefore
incurring a greater per-evaluation cost. The slopes differ across
algorithms, reflecting the fact that each stagnates at a different
local optimum depending on its search strategy, while remaining
non-zero throughout the budget because the landscape barrier (whose depth is a fixed property of the problem) prevents any
algorithm from closing the gap. This illustrates a key advantage of
\acrshort{cr} over \acrshort{ttfo}: even when convergence is never
observed, the regret trajectory continues to distinguish algorithms
by the quality of their trapped incumbent, information that a binary
optimum achievement measure cannot provide.

!TODO: mention MWU and other stats from regret boxplots!

\subsubsection{Trajectory Analysis}
% history_best, regret_instantaneous, regret_instantaneous_best,
% regret_cumulative_best per problem
% Focus: shape differences across unimodal / deceptive / exponential problems

\subsection{Suite 02: Extended Pseudo-Boolean Benchmark}
\label{sec:results:suite02}

\subsubsection{Aggregate Regret and Convergence}

\subsubsection{Effect of Algorithm Overrides}
% RLSExploration (eps=0.05, no decay) on Jump/Trap vs. default behaviour
% SA d=6.0 on Jump-5 / Trap-5 vs. d=2.0

\subsubsection{Trajectory Analysis}
% Harder problems: HIFF-64, Petersen, Plateau-10 - convergence or saturation?

\section{Verification Suite}
\label{sec:results:verification}

\subsection{Suite 08: Runtime Profile Verification}
\label{sec:results:suite08}

\subsubsection{Tail-Sum Identity: Direct vs.\ Profile-Derived
\texorpdfstring{$\mathbb{E}[\mathrm{CR}(T)]$}{E[CR(T)]}}
% cr_profile_verification for RLS and (1+1)-EA on OneMax and LeadingOnes
% Quantify residual gap; attribute to discretisation or numerical error

\subsubsection{Inverse Runtime Profile Surfaces and Curves}
% inverse_runtime_profile_surface per algorithm
% inverse_runtime_profile_curves at selected fitness levels

\section{Exploratory Analysis}
\label{sec:results:exploratory}

\subsection{Barrier Structure and Mutation Rate}
\label{sec:results:barrier}

\subsubsection{Suite 04: Trap -- Deception Width and Mutation Rate}
% Does cumulative regret shape change with k (not just final regret)?
% Does SA escape deceptive basins where the EA cannot?

\subsubsection{Suite 05: Jump -- Mutation Rate and Plateau Onset}
% Instantaneous regret plateau as diagnostic of trapped phase
% Adaptive k/n vs. fixed rates; comparison with TTFO when P(opt)~0

\subsubsection{Suite 06: Plateau -- Neutral Walk and SA}
% Jump vs. Plateau cumulative regret shape at shared k=2, k=5
% Does SA benefit on neutral landscapes?

\subsubsection{Suite 09: Jump -- Plateau Duration}
% Extended horizon: plateau duration scales as Theta(n^k)?
% Plateau exit at adaptive rate for small k

\subsubsection{Suite 10: Barrier Archetype Comparison at k=5}
% Trap vs. Jump vs. Plateau cumulative regret: early deception,
% delayed crossing, neutral trapping
% TTFO insufficiency at P(opt)~0 vs. regret trajectory informativeness

\subsection{NK Ruggedness and SA Cooling}
\label{sec:results:nk}

\subsubsection{Suite 03: Cooling Schedule × Epistasis Grid}
% 6x5 grid results: which d value performs best at each k?
% Crossover levels in profile curves; saturation frontier in profile surface
% RLS / (1+1)-EA baselines: does any SA variant improve on
% hill-climbing at low k?

\subsubsection{Suite 11: SA Cooling Schedule Diagnostics}
% Instantaneous regret trajectory signatures by schedule family
% Logarithmic (sustained positive T) vs. linear/exponential (collapse
% to hill-climber)
% Schedules underperforming RLS identified via pairwise annotations

\subsubsection{Suite 12: NK Saturation}
% Regret saturation floor vs. k; inverse runtime profile saturation frontier
% Per-problem d tuning: does adequate temperature maintenance change
% saturation outcome?

\subsection{Hierarchical Structure and Scaling}
\label{sec:results:scaling}

\subsubsection{Suite 07: HIFF Scaling with Problem Size}
% RLS saturation at first hierarchical level (n>=64)
% EA mutation rate sensitivity at each n
% SA trajectory on hierarchical sub-block barriers

\subsubsection{Suite 13: Regret Scaling with n on OneMax}
% E[CR(T)] scaling exponent: theoretical Theta(n) vs. empirical estimate
% Log-log regret curves: rightward shift of ~log(2) per doubling of n

% \subsection{Population Dynamics}
% \label{sec:results:population}

% \subsubsection{Suite 14: (mu+lambda)-EA vs. (1+1)-EA}
% (1+1)-EA vs. (1+1)-EA-framework: implementation consistency check
% Population overhead on unimodal problems (OneMax, LeadingOnes)
% Diversity benefit on multimodal problems (TwoMax, Jump-3)
% Selection pressure ratio (mu/lambda) effect on cumulative regret shape

\section{Cross-Suite Synthesis}
\label{sec:results:synthesis}

\subsection{Regret Trajectory Shapes as Landscape Diagnostics}
% Summarise the diagnostic signatures identified across suites:
% flat floor (saturation), linear ramp (barrier), smooth decay (unimodal)

\subsection{When TTFO Fails and Cumulative Regret Succeeds}
% Consolidate evidence from suites 05, 09, 10 where P(opt)~0 renders
% TTFO uninformative but regret trajectories remain structured

\subsection{Runtime Profiles and Regret: Complementary Views}
% Connect suite 08 verification to suite 03/12 profile surfaces
% Practical value of the identity E[CR(T)] = sum_v sum_t P(tau_v > t)
